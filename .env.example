# Config de instância do TabelaPort — copie para .env e preencha.
# Este arquivo é versionado como exemplo; .env fica gitignored.

# Caminho do repo da instância (portfolio) que recebe o código deste template.
# PORTFOLIO_DIR=$HOME/codigo/pessoal/portfolio

# Pasta onde copiar os CVs gerados durante o --update.
# DOCS_DIR=$HOME/Documents/profissionais
